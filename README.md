# The Music of Memory

Static educational site about music, emotion, and Alzheimer’s disease. Open `index.html` in a browser via a local server (required for consistent asset loading).

```bash
cd /Users/kumikoueda/GitHub/music-alzheimers
python3 -m http.server 8080
```

Then visit `http://127.0.0.1:8080/`.